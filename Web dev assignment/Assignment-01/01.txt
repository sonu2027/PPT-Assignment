1. <!DOCTYPE html> is it a tag of html? If not, what is it and why do we use it?

Ans. <!DOCTYPE html> is not a tag of HTML. It is a document type declaration used to specify that the document is written in HTML5. It ensures proper rendering and interpretation of the HTML code by web browsers.